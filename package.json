{
  "name": "tell-ai-monorepo",
  "private": true,
  "version": "0.5.0",
  "description": "Monorepo for the tell-ai CLI, the @tell-ai/sdk browser-safe AI library, and the @tell-ai/web sandbox server",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter @tell-ai/sdk build && bun run --filter tell-ai build && bun run --filter @tell-ai/web build",
    "lint": "bun run --filter @tell-ai/sdk lint && bun run --filter tell-ai lint && bun run --filter @tell-ai/web lint" ,
    "format": "biome check --write packages",
    "check": "biome check packages",
    "test:security": "bun run --filter @tell-ai/sdk build && node test/test-tell-security.js",
    "test:context": "bun run --filter @tell-ai/sdk build && node test/test-tell-context.js",
    "test:web": "node test/test-web-backend.js && bun run --filter @tell-ai/web test",
    "test": "bun run test:security && bun run test:context && bun run test:web",
    "ci": "bun run build && bun run lint && bun run check && bun run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/naoeosavio/Tell-ai.git"
  },
  "author": "it is not savio",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.2.6",
    "@types/node": "26.1.0",
    "tsup": "8.5.1",
    "typescript": "^5.9.3"
  }
}
